getClasses(elem){
  var classes = elem.className;
  /*split substr by spaces*/
  /*save to an array*/
}

addClass(elem, className){
  var classes = getClasses(elem);
  /*check the array to see if className is in the list*/
  /*if it is, don't add it, otherwise add the classes from elem.className along with the new className*/
}

validateForm(){
/* checks that all text fields have entries and that the password and confirm password entries are the same. If there are any errors have the validateForm */
}